# Build artifacts
target/
build/
bin/
dist/
out/
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# Language toolchain caches
node_modules/
.pnpm-store/
.yarn/
__pycache__/
*.py[cod]
.venv/
venv/
.cargo/
.gradle/
.go/

# Editor / IDE
.idea/
*.iml
.vscode/
!.vscode/extensions.json
*.swp
*.swo
.DS_Store

# Local model cache and runtime artifacts (never commit weights).
# Anchor to repo root so this does NOT hide internal/models/ — that
# path is our models catalog Go package, which belongs in the repo.
/models/
*.gguf
*.bin
*.safetensors
*.onnx
.cache/
.intent-cache/

# Logs and audit data
*.log
logs/
audit.jsonl

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# OS
Thumbs.db
ehthumbs.db
Desktop.ini

# Cursor agent state (local-only, do not publish)
.cursor/hooks/state/

# Claude Code worktrees (local-only scratch space)
.claude/worktrees/

# Personal agent memory — AGENTS.md is maintained locally by the
# continual-learning skill and contains developer-specific preferences,
# workspace facts, and derived context from private chat transcripts.
# Never publish in this public repo.
AGENTS.md
